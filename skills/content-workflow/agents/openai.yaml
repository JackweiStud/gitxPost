interface:
  display_name: "Content Workflow"
  short_description: "Draft Markdown articles with prompts and image flow"
  default_prompt: "Use gitxPost's content workflow to scaffold an article, choose the right prompt style, validate the markdown, and prepare it for publish."
