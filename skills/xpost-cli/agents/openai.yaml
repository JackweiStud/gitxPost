interface:
  display_name: "gitxPost CLI"
  short_description: "Draft and publish X Articles or Posts"
  default_prompt: "Use the local gitxPost CLI to prepare, validate, draft, or publish an X Article or X Post."
